<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>TalkDesk | Streamlined Team Communication</title>

  <!-- Stisla CSS -->
  <link rel="stylesheet" href="/dist/assets/modules/bootstrap/css/bootstrap.min.css" />
  <link rel="stylesheet" href="/dist/assets/modules/fontawesome/css/all.min.css" />
  <link rel="stylesheet" href="/dist/assets/css/style.css" />
  <link rel="stylesheet" href="/dist/assets/css/components.css" />

  <style>
    body {
      background-color: #0e1015;
      color: #ffffff;
      font-family: 'Segoe UI', sans-serif;
      margin: 0;
    }

    .hero {
      padding: 100px 20px 60px;
      text-align: center;
      background: linear-gradient(120deg, #1f1f2e, #0e1015);
    }

    .hero h1 {
      font-size: 3.5rem;
      font-weight: 700;
      margin-bottom: 20px;
      line-height: 1.3;
    }

    .hero p {
      font-size: 1.2rem;
      opacity: 0.8;
      max-width: 650px;
      margin: 0 auto 40px;
    }

    .hero-buttons a {
      margin: 0 10px;
    }

    /* أزرار فخمة */
    .btn-cta {
      padding: 14px 36px;
      font-size: 1.05rem;
      border: none;
      border-radius: 999px;
      font-weight: 600;
      transition: all 0.3s ease-in-out;
      display: inline-flex;
      align-items: center;
      gap: 10px;
      text-transform: capitalize;
      letter-spacing: 0.5px;
      box-shadow: 0 8px 20px rgba(79, 172, 254, 0.25);
    }

    .btn-cta-primary {
      background: linear-gradient(135deg, #4facfe, #00f2fe);
      color: #fff;
    }

    .btn-cta-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 25px rgba(79, 172, 254, 0.4);
    }

    .btn-cta-outline {
      background: transparent;
      border: 2px solid #fff;
      color: #fff;
    }

    .btn-cta-outline:hover {
      background: #ffffff15;
      transform: translateY(-2px);
      box-shadow: 0 10px 20px rgba(255, 255, 255, 0.1);
    }

    .features {
      padding: 80px 20px;
      background-color: #12141d;
      text-align: center;
    }

    .features h2 {
      font-size: 2.5rem;
      margin-bottom: 40px;
    }

    .feature-grid {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 30px;
    }

    .feature-card {
      background-color: #1e1f29;
      padding: 30px;
      border-radius: 16px;
      width: 300px;
      text-align: left;
      box-shadow: 0 0 10px rgba(0,0,0,0.4);
      transition: 0.3s ease;
    }

    .feature-card:hover {
      transform: translateY(-5px);
    }

    .feature-card i {
      font-size: 1.8rem;
      color: #4facfe;
      margin-bottom: 15px;
    }

    .feature-card h4 {
      font-size: 1.2rem;
      margin-bottom: 10px;
    }

    .cta {
      padding: 60px 20px;
      background-color: #181a23;
      text-align: center;
    }

    .cta h3 {
      font-size: 2rem;
      margin-bottom: 20px;
    }

    .footer {
      text-align: center;
      padding: 20px;
      font-size: 0.9rem;
      color: #888;
    }

    @media (max-width: 768px) {
      .hero h1 {
        font-size: 2.5rem;
      }

      .feature-card {
        width: 90%;
      }

      .btn-cta {
        width: 100%;
        justify-content: center;
      }

      .hero-buttons {
        flex-direction: column;
        gap: 15px;
        display: flex;
        align-items: center;
      }
    }
    .logo {
  width: 200px; 
  height: auto; 
  margin-bottom: -90px; 
  margin-top: -40px;
}
  </style>
</head>
<body>

  <!-- Hero Section -->
  <section class="hero">
    <img src="/assets/logo-dark.png" alt="TalkDesk Logo" class="logo  mb-4" />
    <h1>Next-Gen Internal Chat for Smart Teams</h1>
    <p>
      Empower your team with lightning-fast messaging, smart channels, and seamless collaboration.
      TalkDesk is built for modern companies who care about communication.
    </p>
    <div class="hero-buttons">
      <a href="/register-company" class="btn-cta btn-cta-primary">
        <i class="fas fa-building"></i> Register Company
      </a>
      <a href="/login" class="btn-cta btn-cta-outline">
        <i class="fas fa-sign-in-alt"></i> Login
      </a>
    </div>
  </section>

  <!-- Features Section -->
  <section class="features">
    <h2>Why TalkDesk?</h2>
    <div class="feature-grid">
      <div class="feature-card">
        <i class="fas fa-shield-alt"></i>
        <h4>Secure by Design</h4>
        <p>Your company's communication stays private and encrypted. You own your data.</p>
      </div>
      <div class="feature-card">
        <i class="fas fa-bolt"></i>
        <h4>Real-Time Messaging</h4>
        <p>Instant delivery, read receipts, typing indicators, and more. Speed matters.</p>
      </div>
      <div class="feature-card">
        <i class="fas fa-layer-group"></i>
        <h4>Structured Channels</h4>
        <p>Organize your teams into channels, departments, or topics to keep discussions focused.</p>
      </div>
      <div class="feature-card">
        <i class="fas fa-user-shield"></i>
        <h4>Granular Roles</h4>
        <p>Admins, employees, supervisors — full control over access and actions inside your org.</p>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="cta">
    <h3>Ready to transform your team's communication?</h3>
    <a href="/auth/register" class="btn-cta btn-cta-primary">
      <i class="fas fa-rocket"></i> Get Started Now
    </a>
  </section>

  <!-- Footer -->
  <div class="footer">
    &copy; <%= new Date().getFullYear() %> TalkDesk. Built for Teams.
  </div>

  <!-- Scripts -->
  <script src="/dist/assets/modules/jquery.min.js"></script>
  <script src="/dist/assets/modules/bootstrap/js/bootstrap.min.js"></script>
</body>
</html>
