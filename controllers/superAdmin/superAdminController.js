const {getPendingCompanies, viewPendingCompany ,approveCompany, rejectCompany } = require('./companies');

module.exports = {
    getPendingCompanies,
    viewPendingCompany,
    approveCompany,
    rejectCompany
}
